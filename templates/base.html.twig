<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8"/>
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"/>
    {% block stylesheets %}{% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="#">symfony-task</a>
        <a class="btn btn-light" href="{{ path('tree') }}">Tree</a>
        <div class="collapse navbar-collapse" id="navbarSupportedContent"></div>
        <ul class="nav navbar-nav navbar-right">
            {% if app.user %}
                <li><a class="btn btn-primary" href="{{ path('app_logout') }}">Logout</a></li>
            {% else %}
                <li><a class="btn btn-primary" href="{{ path('app_login') }}">Login</a></li>
                <li class="ml-1"><a class="btn btn-secondary " href="{{ path('user_register') }}">Register</a></li>
            {% endif %}
        </ul>
    </div>
</nav>
{% block body %}{% endblock %}

{% block javascripts %}{% endblock %}
</body>
</html>